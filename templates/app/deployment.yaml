{{- if and .Values.app.enabled (eq .Values.app.enabled true) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  labels:
    app: nginx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: nginx:{{ .Values.app.nginx.tag }}
          ports:
            - containerPort: 80
{{- end }}

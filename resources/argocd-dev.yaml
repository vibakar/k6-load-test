apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-dev
  namespace: argocd
  labels:
    app.kubernetes.io/managed-by: argocd
    app: nginx-dev
  annotations:
    kargo.akuity.io/authorized-stage: nginx:dev
spec:
  project: default
  source:
    repoURL: https://github.com/vibakar/k6-load-test.git
    targetRevision: main
    path: chart
    helm:
      valueFiles:
        - values.dev.yaml
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
